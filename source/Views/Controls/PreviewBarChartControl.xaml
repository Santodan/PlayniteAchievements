<UserControl x:Class="PlayniteAchievements.Views.Controls.PreviewBarChartControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             x:Name="Root"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="280">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/PlayniteAchievements;component/Resources/AchievementTemplates.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Mock time range buttons (static preview) -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Border Background="{DynamicResource HighlightGlyphBrush}" CornerRadius="3" Padding="6,2" Margin="0,0,4,0">
                <TextBlock Text="7D" FontSize="{DynamicResource FontSizeTiny}" Foreground="White"/>
            </Border>
            <Border Background="{DynamicResource ControlBackgroundBrush}" CornerRadius="3" Padding="6,2" Margin="0,0,4,0" BorderBrush="{DynamicResource NormalBorderBrush}" BorderThickness="1">
                <TextBlock Text="14D" FontSize="{DynamicResource FontSizeTiny}" Foreground="{DynamicResource TextBrush}"/>
            </Border>
            <Border Background="{DynamicResource ControlBackgroundBrush}" CornerRadius="3" Padding="6,2" Margin="0,0,4,0" BorderBrush="{DynamicResource NormalBorderBrush}" BorderThickness="1">
                <TextBlock Text="1M" FontSize="{DynamicResource FontSizeTiny}" Foreground="{DynamicResource TextBrush}"/>
            </Border>
        </StackPanel>

        <!-- LiveCharts CartesianChart (actual control) -->
        <lvc:CartesianChart Grid.Row="1"
                            Series="{Binding TimelineSeries, ElementName=Root}"
                            LegendLocation="None"
                            Hoverable="True"
                            DisableAnimations="True">
            <lvc:CartesianChart.Resources>
                <Style TargetType="lvc:ColumnSeries">
                    <Setter Property="Fill" Value="{DynamicResource HighlightGlyphBrush}" />
                </Style>
            </lvc:CartesianChart.Resources>
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Labels="{Binding TimelineLabels, ElementName=Root}"
                          ShowLabels="True"
                          LabelsRotation="0"
                          FontSize="{DynamicResource FontSizeTiny}"
                          Foreground="{DynamicResource TextBrush}">
                    <lvc:Axis.Separator>
                        <lvc:Separator Step="1" IsEnabled="False"/>
                    </lvc:Axis.Separator>
                </lvc:Axis>
            </lvc:CartesianChart.AxisX>
            <lvc:CartesianChart.AxisY>
                <lvc:Axis MinValue="0"
                          LabelFormatter="{Binding YAxisFormatter, ElementName=Root}"
                          FontSize="{DynamicResource FontSizeTiny}"
                          Foreground="{DynamicResource TextBrush}">
                    <lvc:Axis.Separator>
                        <lvc:Separator Stroke="{DynamicResource NormalBorderBrush}" Opacity="0.2"/>
                    </lvc:Axis.Separator>
                </lvc:Axis>
            </lvc:CartesianChart.AxisY>
        </lvc:CartesianChart>
    </Grid>
</UserControl>
