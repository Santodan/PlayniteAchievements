namespace PlayniteAchievements.Models.Settings
{
    /// <summary>
    /// Selects the settled SAN notification view rendered into with-notification screenshots.
    /// Automatic preserves the live animation frame used by older settings files.
    /// </summary>
    public enum SanScreenshotView
    {
        Automatic,
        View1,
        View2
    }
}
